# 变更记录

本项目采用独立的 GitHub Release 标签记录公开版本。软件窗口标题不附加版本后缀。

## 未发布

- 将作者自研代码调整为 PolyForm Noncommercial License 1.0.0，自研美术、文档与资源继续使用 CC BY-NC-SA 4.0，并明确两者按材料类型分域授权而非双重许可。
- 内置用户协议升级为 `2026-08-17-v4`，补充分域许可、第三方许可和历史版本授权边界，并提供两份正式许可的独立入口。
- 在单文件 EXE 中嵌入实际 .NET 10.0.10 运行时的 MIT 许可及第三方通知，并可从用户协议窗口查看。

## v1.0.0-beta.4 - 预发布测试版

- 新增“客户端整合包打包”模块，可读取 Minecraft 游戏目录并选择模组、材质包、光影包、存档、配置及其他客户端数据。
- 客户端整合包支持 Modrinth `.mrpack` 与 CurseForge `.zip`，通过精确文件哈希生成下载清单，未识别文件自动写入 `overrides`。
- 改进服务端纯客户端模组识别、必需前置选择与 Fabric API 判断，减少不适用于服务端的模组被错误打包。
- 服务端会综合 Minecraft 与模组声明选择 Java，启动脚本写入对应 Java 路径，并新增更醒目的 PowerShell 控制台脚本。
- 细化服务端核心和模组下载进度，增加实时下载速度显示，并在缺少输出路径时主动请求用户选择。
- 客户端内容分类支持折叠，将“其他模组数据”合并到“小地图、世界地图及其他 Mod 数据”，保持默认选择和导出路径不变。
- 统一圆角复选框、侧边栏收起与页面布局；修复自定义标题栏窗口可被缩到不可用尺寸的问题，并在不同 DPI 下强制执行 `1120×600` 的最小窗口尺寸。
- 修复整合包文件选择框重复弹出、读取内容覆盖用户输出目录等交互问题。

## v1.0.0-beta.3 - 预发布测试版

- 新增“服务端一键打包”模块，可从 Minecraft 游戏目录或 CurseForge `.zip`、Modrinth `.mrpack` 整合包读取内容并生成服务器端压缩包。
- 支持识别 Minecraft 版本、模组加载器及加载器版本，并自动刷新可用的服务端核心；核心选项会根据加载器和游戏版本进行筛选。
- 支持按需选择服务端模组、复制 `config` 配置和将指定存档完整复制并重命名为 `world`，导出时增加 Minecraft EULA 同意选项。
- 细化服务端模组适用性分类：加载器 API、必需前置和需要客户端/服务端同时安装的模组默认勾选；纯客户端或明确可选的模组默认不勾选，无法在线识别的本地模组保留为可选项。
- 修复服务端导出文件名自动生成时重复追加版本号的问题。
- 修复导入后必需模组和前置未自动勾选的问题。
- 统一自定义弹窗、标题栏和窗口交互样式，改善深浅色主题、表格滚动提示及相关界面细节。

## v1.0.0-beta.2 - 预发布测试版

- 完成从 Python 到 C#（WPF）的整体迁移，保留上一版主要功能和用户协议流程。
- 支持读取 CurseForge `.zip` 和 Modrinth `.mrpack` 整合包，并按目标 Minecraft/加载器环境重新匹配模组、资源包和光影包。
- 支持平台项目 ID、ForgeCDN file ID 和文件哈希校验，生成静态兼容性报告，并保留 `overrides` 中的相对路径内容。
- 支持简体中文、繁体中文（香港）和 English，以及字体、浅色/深色主题和自定义主题色设置。
- 提供 Windows 10/11 x64 单文件 EXE，无需安装 Python 或额外运行库。
- 已知限制：服务端打包模块当时尚未实现；兼容性分析为静态检查；平台接口、限流和网络环境可能导致查询失败。

## v1.0.0-beta.1 - 预发布测试版

- 产品更名为“MC整合包工具”，新增主页和常驻侧边栏，可在主页、版本迁移、服务端打包和设置之间切换。
- 原版本迁移功能迁入独立模块页面，并在切换页面、语言或主题时保留当前迁移状态。
- 设置页支持简体中文、繁体中文（香港）和 English，以及浅色/深色主题、预设或自定义主题色和系统字体切换。
- 主页新增 Bilibili 作者主页、GitHub 项目入口、用户协议入口和“作者：风尘WD”署名。
- 更新工作台 Logo，并同步生成 PNG 与多尺寸 ICO。
- 支持读取 CurseForge `.zip` 和 Modrinth `.mrpack` 整合包。
- 按目标 Minecraft 与加载器环境重新匹配模组、资源包和光影包。
- 优先使用平台项目 ID、ForgeCDN file ID 和文件哈希确认项目身份。
- 增加保守的文本搜索评分，避免把本体模组替换成兼容附属、分支或 Fork 项目。
- 检查可识别的直接依赖、明确冲突、重复项目和输出路径冲突。
- 保持 `overrides` 内容原样打包，不查询、不迁移、不改写。
- 增加可切换语言的首次启动用户协议和自动生成目标包名。
- 提供单文件 Windows EXE，不要求最终用户安装 Python。
- 统一应用版本、网络 User-Agent、GitHub 标签和 Windows EXE 版本资源。
- 为联网下载增加 `Content-Length` 预检、预期大小限制和 2 GiB 流式硬上限。
- 增加可复现发布依赖和完整第三方许可说明。
- 移除公开源码中的内置 CurseForge API Key，源码运行改为读取 `CURSEFORGE_API_KEY` 环境变量。
- 增加手动预发布构建方案：从受保护的 GitHub Environment Secret `CURSEFORGE_API_KEY` 临时生成 `build_secrets.py`，构建后删除且不写入 Git 历史。

### 已知限制

- 兼容性分析是静态检查，不会启动 Minecraft 或执行模组代码。
- 无法穷尽仅在运行时出现的 Mixin、注册表、版本范围及模组内部冲突。
- CurseForge、Modrinth 和加载器服务的网络、限流或接口变化可能导致查询失败。
- GitHub Secret 不能保护已经写入客户端 EXE 的 CurseForge Key；发布 Key 仍可能被提取，必须独立、可撤销并定期监控和轮换。
- 这是测试版，导出的整合包仍应在备份实例中实际启动验证。
