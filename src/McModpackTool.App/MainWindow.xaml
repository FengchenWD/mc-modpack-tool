<Window x:Class="McModpackTool.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        xmlns:ui="clr-namespace:McModpackTool.App.UI"
        Title="{Binding [app.name]}" Width="1280" Height="800" MinWidth="1120" MinHeight="600"
        WindowStartupLocation="CenterScreen" WindowStyle="None" ResizeMode="CanResize"
        Foreground="{DynamicResource TextBrush}" FontFamily="{DynamicResource AppFontFamily}"
        UseLayoutRounding="True" SnapsToDevicePixels="True">
    <Window.Resources>
        <Style x:Key="NavStyle" TargetType="RadioButton">
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Height" Value="44" />
            <Setter Property="Margin" Value="8,2" />
            <Setter Property="Padding" Value="13,0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="Root" Background="{TemplateBinding Background}" CornerRadius="6">
                            <Grid>
                                <Border x:Name="HoverBackground" CornerRadius="6" IsHitTestVisible="False"
                                        Background="{DynamicResource SidebarHoverBrush}" Opacity="0" />
                                <Border x:Name="SelectionBackground" CornerRadius="6" IsHitTestVisible="False"
                                        Background="{DynamicResource AccentSoftBrush}" Opacity="0" />
                                <Grid Margin="{TemplateBinding Padding}">
                                    <Grid.ColumnDefinitions><ColumnDefinition Width="28" /><ColumnDefinition /></Grid.ColumnDefinitions>
                                    <Border x:Name="SelectionIndicator" Width="3" Height="18" Margin="-10,0,0,0"
                                            HorizontalAlignment="Left" VerticalAlignment="Center" CornerRadius="2"
                                            Background="{DynamicResource AccentBrush}" Opacity="0" />
                                    <TextBlock FontFamily="Segoe Fluent Icons" FontSize="16" VerticalAlignment="Center" TextAlignment="Center" Text="{TemplateBinding Tag}" />
                                    <ContentPresenter Grid.Column="1" VerticalAlignment="Center" Margin="5,0,0,0" />
                                </Grid>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsMouseOver" Value="True" />
                                    <Condition Property="IsChecked" Value="False" />
                                </MultiTrigger.Conditions>
                                <MultiTrigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="HoverBackground" Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.10" />
                                        </Storyboard>
                                    </BeginStoryboard>
                                </MultiTrigger.EnterActions>
                                <MultiTrigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="HoverBackground" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.08" />
                                        </Storyboard>
                                    </BeginStoryboard>
                                </MultiTrigger.ExitActions>
                            </MultiTrigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter Property="FontWeight" Value="SemiBold" />
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="SelectionBackground" Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.14" />
                                            <DoubleAnimation Storyboard.TargetName="SelectionIndicator" Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.14" />
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="SelectionBackground" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.10" />
                                            <DoubleAnimation Storyboard.TargetName="SelectionIndicator" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.10" />
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="CaptionButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="46" />
            <Setter Property="Height" Value="40" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="FontFamily" Value="Segoe Fluent Icons" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Cursor" Value="Arrow" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="CaptionRoot" Background="Transparent">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="CaptionRoot" Property="Background" Value="{DynamicResource SurfaceAltBrush}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="CaptionRoot" Property="Opacity" Value="0.68" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="CaptionCloseButtonStyle" TargetType="Button" BasedOn="{StaticResource CaptionButtonStyle}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="CaptionRoot" Background="Transparent">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="CaptionRoot" Property="Background" Value="#C42B1C" />
                                <Setter Property="Foreground" Value="White" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="CaptionRoot" Property="Background" Value="#A51D13" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="SidebarToggleButtonStyle" TargetType="Button" BasedOn="{StaticResource IconButtonStyle}">
            <Setter Property="Width" Value="40" />
            <Setter Property="Height" Value="40" />
            <Setter Property="MinWidth" Value="40" />
            <Setter Property="MinHeight" Value="40" />
            <Setter Property="MaxWidth" Value="40" />
            <Setter Property="MaxHeight" Value="40" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="HorizontalContentAlignment" Value="Center" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>
    </Window.Resources>

    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="40" ResizeBorderThickness="6" GlassFrameThickness="0"
                            CornerRadius="0" UseAeroCaptionButtons="False" />
    </shell:WindowChrome.WindowChrome>

    <Grid x:Name="UiRoot" Background="{DynamicResource AppBackgroundBrush}">
        <Grid.RowDefinitions><RowDefinition Height="40" /><RowDefinition Height="*" /></Grid.RowDefinitions>

        <ui:AppTitleBar x:Name="MainTitleBar" Title="{Binding [app.name]}"
                        MinimizeVisibility="Visible" MaximizeVisibility="Visible" />

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions><ColumnDefinition x:Name="SidebarColumn" Width="176" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>

            <Border x:Name="SidebarBorder" Background="{DynamicResource SidebarBrush}" BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="0,0,1,0" ClipToBounds="True">
                <Grid>
                <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="*" /><RowDefinition Height="Auto" /></Grid.RowDefinitions>
                <StackPanel x:Name="SidebarBrandPanel" Margin="18,20,14,18">
                    <Grid>
                        <Grid.ColumnDefinitions><ColumnDefinition Width="38" /><ColumnDefinition /></Grid.ColumnDefinitions>
                        <Image Width="34" Height="34" Source="Assets/mc_modpack_tool.png" Stretch="Uniform" />
                        <StackPanel x:Name="SidebarBrandText" Grid.Column="1" Margin="7,0,0,0" VerticalAlignment="Center">
                            <TextBlock FontWeight="SemiBold" FontSize="13" Text="{Binding [app.name]}" TextTrimming="CharacterEllipsis" />
                            <TextBlock FontSize="10" Foreground="{DynamicResource MutedTextBrush}" Text="{Binding [app.short_subtitle]}" />
                        </StackPanel>
                    </Grid>
                </StackPanel>

                <StackPanel Grid.Row="1">
                    <RadioButton x:Name="HomeNav" GroupName="Navigation" IsChecked="True" Tag="&#xE80F;" Style="{StaticResource NavStyle}" Click="Nav_Click" CommandParameter="home" ToolTip="{Binding [nav.home]}" AutomationProperties.Name="{Binding [nav.home]}"><TextBlock x:Name="HomeNavText" Text="{Binding [nav.home]}" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis" /></RadioButton>
                    <RadioButton x:Name="MigrationNav" GroupName="Navigation" Tag="&#xE8AB;" Style="{StaticResource NavStyle}" Click="Nav_Click" CommandParameter="migration" ToolTip="{Binding [nav.migration]}" AutomationProperties.Name="{Binding [nav.migration]}"><TextBlock x:Name="MigrationNavText" Text="{Binding [nav.migration]}" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis" /></RadioButton>
                    <RadioButton x:Name="ClientPackNav" GroupName="Navigation" Tag="&#xE7B8;" Style="{StaticResource NavStyle}" Click="Nav_Click" CommandParameter="client_pack" ToolTip="{Binding [nav.client_pack]}" AutomationProperties.Name="{Binding [nav.client_pack]}"><TextBlock x:Name="ClientPackNavText" Text="{Binding [nav.client_pack]}" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis" /></RadioButton>
                    <RadioButton x:Name="ServerNav" GroupName="Navigation" Tag="&#xE950;" Style="{StaticResource NavStyle}" Click="Nav_Click" CommandParameter="server" ToolTip="{Binding [nav.server]}" AutomationProperties.Name="{Binding [nav.server]}"><TextBlock x:Name="ServerNavText" Text="{Binding [nav.server]}" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis" /></RadioButton>
                </StackPanel>

                <StackPanel Grid.Row="2" Margin="0,0,0,8">
                    <Separator Margin="14,0,14,7" Background="{DynamicResource BorderBrush}" />
                    <RadioButton x:Name="SettingsNav" GroupName="Navigation" Tag="&#xE713;" Style="{StaticResource NavStyle}" Click="Nav_Click" CommandParameter="settings" ToolTip="{Binding [nav.settings]}" AutomationProperties.Name="{Binding [nav.settings]}"><TextBlock x:Name="SettingsNavText" Text="{Binding [nav.settings]}" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis" /></RadioButton>
                    <Button x:Name="SidebarToggleButton" Margin="8,2" HorizontalAlignment="Right"
                            Style="{StaticResource SidebarToggleButtonStyle}" Click="SidebarToggle_Click">
                        <TextBlock x:Name="SidebarToggleIcon" Width="18" Height="18"
                                   HorizontalAlignment="Center" VerticalAlignment="Center"
                                   FontFamily="Segoe Fluent Icons" FontSize="14" LineHeight="18"
                                   LineStackingStrategy="BlockLineHeight" TextAlignment="Center"
                                   Text="&#xE76B;" />
                    </Button>
                </StackPanel>
                </Grid>
            </Border>

            <ScrollViewer x:Name="ContentViewport" Grid.Column="1"
                          HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled"
                          HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch"
                          CanContentScroll="False" Focusable="False">
                <ContentControl x:Name="ContentHost" MinWidth="760"
                                Width="{Binding ViewportWidth, ElementName=ContentViewport, Mode=OneWay}"
                                HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch"
                                RenderTransformOrigin="0.5,0.5">
                    <ContentControl.RenderTransform><TranslateTransform /></ContentControl.RenderTransform>
                </ContentControl>
            </ScrollViewer>
        </Grid>
    </Grid>
</Window>
